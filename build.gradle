version '1.0'

apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'HistoricalReview'

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
